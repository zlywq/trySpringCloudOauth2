server:
  port: 9020
spring:
  application:
    name: sprcldsrv-provider2layer
eureka:
  client:
    serviceUrl:
      defaultZone: http://discovery:8761/eureka/
  instance:
    preferIpAddress: true

logging:
  level:
    root: INFO
    org.springframework.security: DEBUG
    org.springframework.web: DEBUG
    org.springframework: INFO
    sprssozdemo1: DEBUG